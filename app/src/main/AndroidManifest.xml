<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="omdvet.com">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".AddDelegateActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".CustomersActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".CollectionActivity" />
        <activity android:name=".AddCustomerActivity" />
        <activity android:name=".DelegatesActivity" />
        <activity android:name=".AddProductActivity" />
        <activity android:name=".EditProductActivity" />
        <activity android:name=".getOrdersActivity" />
        <activity android:name=".OrderDetailsActivity"></activity>
    </application>

</manifest>